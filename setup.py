from setuptools import setup

setup(
    name="pingumil",
    version="0.0.1",
    description="This package was created apply graph machine learning techniques in game provenance graphs",
    packages=["pingumil"],
    include_package_data=True
    )